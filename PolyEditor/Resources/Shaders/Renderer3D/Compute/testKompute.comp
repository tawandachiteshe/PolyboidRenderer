#version 460

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(std140, binding = 0) buffer AgeSSBO {
   uint ages[ ];
};


void main()
{
	
	if(gl_GlobalInvocationID.x == 23)
	{
		ages[gl_GlobalInvocationID.x] = 199;
	} else {
		ages[gl_GlobalInvocationID.x] = 200 * gl_GlobalInvocationID.x;
	}

	
}